#include <iostream>

#include "rules.hpp"

std::pair<char*, std::streampos> readEntireFile(const char* inputFile);

int main(int argc, const char* argv[]) {
    printf("Hello World!\n");
    return 0;
}